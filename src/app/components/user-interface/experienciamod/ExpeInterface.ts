
// Esto basicamente nos permite manejar el tipo de dato que recibe cada parametro.

export interface ExpeInterface {
  id?: number;
  logo: string;
  posicionYtiempo: string;
  descripcion: string;
}